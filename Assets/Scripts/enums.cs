using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class enums : MonoBehaviour {

	public enum PlayerType {Turtle, Eagle}
	public enum Beam {laser,tractor,slower}
	public enum hud{COUNTDOWN,WEAPON,POINTS}
	public enum hudWeapon{LASER_CANNON_PANEL,BULLET_CANNON_PANEL,SECONDARY_WEAPON_PANEL}
	public enum hudPoints{HEALTH_PANEL,SCORE_PANEL,CRYSTALS_PANEL}

}
